{
  "repository": "https://github.com/frlow/cdc-contracts",
  "author": "Fredrik Löwenhamn <fredrik.lowenhamn@gmail.com>",
  "license": "MIT",
  "packageManager": "yarn@3.1.0",
  "workspaces": [
    "packages/**/*"
  ],
  "devDependencies": {
    "json": "^11.0.0",
    "semver": "^7.3.5"
  },
  "scripts": {
    "start": "yarn workspaces foreach --parallel -i -j unlimited run start",
    "build": "yarn workspaces foreach --parallel -i -j unlimited run build",
    "test": "yarn workspaces foreach -i run test",
    "patch": "yarn workspaces foreach -i version patch",
    "publish-to-npm": "yarn workspace cdc-contracts publish && yarn workspace cdc-contracts-pact publish",
    "postinstall": "yarn workspace cdc-contracts postinstall && yarn workspace cdc-contracts-pact postinstall"
  },
  "files": [],
  "dependencies": {
    "prettier": "^2.4.1"
  }
}
